# Toolbar

> Working document — move the cell's hover tools into the effects toolbar as effects, and turn the
> global crop threshold into a per-cell effect.
> This file is the source of truth for the planned work until implemented,
> then the log of every adjustment made to it afterwards.

---

## Overview

The blur mask (`workfiles/20260925-blur-mask.md`, delivered) introduced the **effects toolbar**,
the **options toolbar** and the **selected cell** (see `RULES.md` § Effects). This workfile builds
on that framework:

- The actions currently drawn on a hovered cell become **effects** of the effects toolbar, acting
  on the **selected cell**, with their settings in the options toolbar.
- Some top-bar settings that act on the whole grid become per-cell effects — first of all the
  **Crop** threshold slider (top left), which today applies to every image.
- The effects row gets a leading **"Effects"** label.

Every moved feature follows `RULES.md` § Effects (state on the cell + image pair, click cycle,
options row, handles on the selected cell only, rendering in `Compositor.DrawCell`).

---

## Inventory

Every feature of the cell and of the bars, with the planned destination. *Proposed* rows await the
answers of `## Open Questions`.

### Cell (hover tools — `UI/GridPreview.cs`, `Tool` enum and sliders)

| # | Feature | Today | Destination |
|---|---|---|---|
| C1 | Rotate left / right | Two hover buttons, quarter turns (`ImageLook.Rotate`) | ➡️ Effect **Rotate** — options: the 4 rotations + a fine-angle slider |
| C2 | Flip horizontal / vertical | Two hover toggles (`ToggleFlipX/Y`) | ➡️ Effect **Flip**, H and V as options |
| C3 | Black & white | Hover toggle (`ToggleGrayscale`) | ➡️ Effect **Black & white**, intensity option |
| C4 | Zoom | Slider along the left edge, mouse wheel, snaps to 100 % | *(proposed: effect **Zoom**, slider in the options — OQ 5)* |
| C5 | Move a zoomed image | Drag inside the cell | *(tied to C4 — OQ 5)* |
| C6 | Reset | Last hover button, shown while the look is not `None` | *(OQ 6)* |
| C7 | Remove (**×**) | Hover button, top right; `Delete` key | *(proposed: stays on the cell — OQ 7)* |
| C8 | Swap (**✥** handle) | 48 px central handle, drag onto another cell | *(proposed: stays on the cell — OQ 7)* |
| C9 | Frames / pages slider | Along the bottom of a hovered animated cell | *(proposed: stays on the cell — OQ 7)* |
| C10 | Blur | Already an effect | ✅ Stays in the effects toolbar |

### Top bar (`UI/MainForm.cs`)

| # | Feature | Today | Destination |
|---|---|---|---|
| T1 | Crop threshold slider + label | Global, 0–50 % by 5 %, default 15 %, applies to every image, also used by `CanvasSizer` | ➡️ Effect **Crop**, per cell, slider in the options; the global slider is removed *(details: OQ 8)* |
| T2 | Carrousel checkbox | Grid-level mode (images rotate between cells) | Stays — acts on the whole grid |

### Other bars

| # | Feature | Where | Destination |
|---|---|---|---|
| B1 | Clear all | Bottom left | Stays |
| B2 | ⚙ Settings (Start with Windows) | Bottom right | Stays |
| B3 | Force as image, Copy, Save, Cancel | Bottom right / status line | Stays |
| L1 | Layout thumbnails, Mirror toggle | Layout strip, left | Stays |
| D1 | Add images drop zone | Right of the preview | Stays |

---

## Effects Toolbar

- A leading **"Effects"** label, at the start of the row, before the first button. It is a plain
  label: never pressed, never disabled-looking beyond the row's own state.
- The buttons follow `RULES.md` § Effects Toolbar (pressed = active on the selected cell; disabled
  with no cell selected; click cycle inactive → active + selected → deselected… → deactivated).
- Button order *(proposed — OQ 9)*.
- Consequence of moving the tools: every moved action now needs a **selected cell**. Hovering a
  cell no longer shows its rotate / flip / B&W / reset buttons.

## Effect: Rotate

- Options row: the **4 rotations** (0°, 90°, 180°, 270°), as mutually exclusive buttons, plus a
  **fine-angle slider**, by **1°** steps from **−45° to +45°**, added to the chosen quarter turn.
- The rotation button is **pressed when the angle falls exactly on it** (fine offset 0°); with any
  other offset, no rotation button is pressed. Clicking a rotation button sets that angle and
  brings the slider back to 0°.
- At a non-right angle, the image is **zoomed just enough to cover the cell**: no corner of the
  cell is left uncovered, as with a photo editor's *Straighten* tool.
- Replaces the rotate-left / rotate-right hover buttons.

## Effect: Flip

- One **Flip** effect; its options hold two toggles, **Horizontal** and **Vertical**.
- Replaces the two flip hover toggles.

## Effect: Black & White

- Options: an **intensity** slider, desaturating from 0 to 100 %.
- Replaces the black & white hover toggle.

## Background Color

- The band color keeps today's rule (`README.md` § Fitting rules): the image's own background when
  at least three sides carry one uniform color, otherwise the most frequent color of the image.
- It is **recomputed from the part the cell actually shows** once every effect is applied — fine
  rotation and per-cell crop included, as it already follows zoom and focus.

## Effect: Crop

- Per-cell version of today's crop threshold: same scale, 0–50 % by 5 %.
- Replaces the global Crop slider of the top bar.
- `CanvasSizer` computes the canvas width from each cell's own threshold.

---

## Test Impact

No test project exists in the repository today (`git ls-files` holds no `*Tests*` project).
Whether one is created for this workfile is OQ 11.

| Behaviour to pin | Test file | Create / Update |
|---|---|---|
| *(pending OQ 11)* | | |

---

## Open Questions

- [x] ~~1. Rotate — fine-angle slider: step and range?~~ → 1°, −45° to +45° on top of the quarter turn; the rotation button whose angle is reached is pressed
- [x] ~~2. Rotate — at a non-right angle, the image's corners leave triangles of the cell uncovered: how are they filled?~~ → Auto zoom to cover the cell
- [x] ~~3. Flip — one effect with H / V options, or two separate effects?~~ → One Flip effect, H / V as options
- [x] ~~4. Black & white — an effect with no options, or with an intensity option?~~ → With an intensity option
- [ ] 5. Zoom — does it become an effect (slider in the options)? What happens to the mouse wheel and to dragging to move?
- [ ] 6. Reset — where does it go?
- [ ] 7. Remove (×), swap (✥), frames / pages slider — do they stay on the cell?
- [ ] 8. Crop — which threshold applies to a cell where the Crop effect is inactive?
- [ ] 9. Order of the buttons in the effects toolbar?
- [ ] 10. Deactivating an effect that transforms the image (Rotate, Flip, Zoom, Crop): does it restore the default (0°, no flip, 100 %, default threshold)?
- [ ] 11. Unit tests — create a test project for the pure logic (angle, per-cell crop), or no tests?

---

## Design Iterations

Chronological log of design refinements, of the choices the implementation run
took on its own — flagged `🧭 Implementation choices` — and of every adjustment
requested afterwards — flagged `⚙️ Post-implementation`. One entry per request,
in the order the requests were made.

### Iteration 1 — 2026-09-25

- Scoping answers: moved features act on the **selected cell**; the moved cell tools **are
  effects** (e.g. Rotate with the 4 rotations as options plus a fine-angle slider); full inventory
  from the code, one decision per row; subject judged straightforward (single scout pass).
- User request during exploration: a leading **"Effects"** label on the effects toolbar.
- Inventory written (cell hover tools, top bar, bottom bar, layout strip, drop zone), with a
  proposed destination per row; the open points listed in `## Open Questions`.

### Iteration 2 — 2026-09-25

- OQ 1–4 answered: Rotate gets a 1° fine-angle slider (−45° to +45°) with the matching rotation
  button pressed, and an auto zoom covering the cell; one Flip effect with H / V options; Black &
  white gets an intensity slider. Domain sections added for Rotate, Flip and Black & White.

### Iteration 3 — 2026-09-25

- User request: the band color is recomputed with the same rules as before (uniform sides, else
  dominant color), from the part the cell shows once the effects apply. Section *Background
  Color* added.

---

## Implementation Log

Which delivery steps are done, and in which iteration. A step that does not apply
says so rather than staying blank.

| Step | Iteration | Date | Notes |
|---|---|---|---|
| Code | | | |
| Unit tests | | | |
| README | | | |

---

## Q&A Log

Questions asked by the agent during design, with user responses.

| # | Question | Answer | Date |
|---|---|---|---|
| 1 | What does a feature moved from the cell to the toolbar act on? | The selected cell | 2026-09-25 |
| 2 | Where do the moved cell features that are not effects (rotation, zoom, reset…) go? | They **are** effects — e.g. Rotate with the 4 rotations as options, plus a slider to turn by finer steps | 2026-09-25 |
| 3 | Inventory: list every cell and toolbar feature from the code, then decide row by row? | Yes, full inventory | 2026-09-25 |
| 4 | Straightforward or tricky / long to explore? | Straightforward | 2026-09-25 |
| 5 | Rotate — fine-angle slider: step and range? | 1°, −45° to +45°; moreover, when the slider's angle matches a button, that button gets selected | 2026-09-25 |
| 6 | Rotate — how are the uncovered corners filled at a non-right angle? | Auto zoom to cover | 2026-09-25 |
| 7 | Flip — one effect with H / V options, or two effects? | One Flip effect, H / V as options | 2026-09-25 |
| 8 | Black & white — with or without an intensity option? | With an intensity | 2026-09-25 |
| 9 | Zoom — effect? Wheel and drag-to-move? | | |
| 10 | Reset — where does it go? | | |
| 11 | ×, ✥, frames / pages slider — stay on the cell? | | |
| 12 | Crop — threshold of a cell where the effect is inactive? | | |
| 13 | Order of the effects buttons? | | |
| 14 | Deactivating a transforming effect restores its default? | | |
| 15 | Unit tests — create a test project, or none? | | |

---

*Last updated: 2026-09-25*
