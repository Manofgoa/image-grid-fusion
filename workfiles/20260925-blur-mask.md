# Blur Mask

> Working document — a per-cell "Effects" entry point, whose first effect blurs a rectangle of the
> cell delimited by four draggable bars.
> This file is the source of truth for the planned work until implemented,
> then the log of every adjustment made to it afterwards.

---

## Overview

Add an always-visible **effects toolbar** at the top of the window, reflecting the effects of the
selected image. Selecting an effect opens a second toolbar, below the first, holding that effect's
options. Clicking the selected effect again removes it.

The first effect is a **bar blur**: two horizontal and two vertical bars delimit a rectangle of
the cell. Each side moves on its own (top / right / bottom / left), so the user keeps exactly the
rectangle to blur. A bar dragged close to the cell edge **snaps fully to it**, so no 1–2 px unblurred
strip is left along that edge.

Agreed at scoping (Q&A #1–#3):

- The effect is **per cell**: each cell carries its own effects; the rectangle lives in that cell.
- The blur options are an **intensity** and a **type** (gaussian blur or pixelate).
- The blur shows in the **on-screen preview**, in the **image export**, and on **videos during
  playback**, in real time. It is **not persisted** — the app keeps no session, and this effect
  adds none.

Relevant code (scout pass, Iteration 1):

| Concern | Where |
|---|---|
| Per-cell state | `Composition/ImageLook.cs` — immutable record (rotation, flips, grayscale, zoom, focus), carried by `SourceImage.Look`, changed through `GridPreview.SetLook` |
| Per-cell hover toolbar | `UI/GridPreview.cs` — `enum Tool` (rotate, flip, grayscale, reset), `ToolBounds` / `ToolSlot` / `ToolAt`, `PaintToolbar` / `PaintToolButton`, owner-drawn along the top of the hovered cell |
| Selected cell | `GridPreview._selected` (private `int`) — to expose, with a change notification, for the toolbar |
| Main window top bar | `UI/MainForm.cs` — `TableLayoutPanel top` (threshold slider, label, carousel checkbox), docking order commented where the controls are added |
| Hit-test, drag, cursor | `GridPreview.OnMouseDown` / `OnMouseMove` / `OnMouseUp`, `UpdateHover` (one place sets the cursor), `*Bounds()` methods shared by paint and hit-test |
| Screen ↔ image mapping | `FitCalculator.Compute(cell, size, threshold, zoom, focus)` → `Source` / `Destination` |
| Single render choke point | `Compositor.DrawCell` — used by the preview (`GridPreview.cs:1081`), the still export and the animated export (`GridExport`); video frames reach it on every repaint through `AnimationPlayer` → `SourceImage.Bitmap` |
| Owner-drawn strip precedent | `UI/LayoutStrip.cs` |

---

## UI — Effects Toolbar and Options Toolbar

- **No Effects button, nothing in the cell** (Q&A #5, #6): the effects toolbar lives in the main
  window's top area and is **always visible**. It reflects the effects of the **selected image**
  (`GridPreview._selected`): each effect is a toggle button, **pressed** when that effect is active
  on the selected cell. The cell's hover toolbar is unchanged.
- **Effects toolbar**: one toggle button per available effect (only *Blur* for now), on a **new
  row below the existing top bar** (threshold slider, carousel checkbox) (Q&A #18).
- **Click semantics** (Q&A #7):

  | Effect state on the selected image | Click does |
  |---|---|
  | Inactive | Activates it with its defaults and selects it (options shown) |
  | Active, not selected | Selects it (options shown) |
  | Active and selected | Deactivates it (options hidden) |

- **Options toolbar**: a row shown below the effects row while an effect is selected. For the
  blur: a type switch (*Gaussian* / *Pixelate*) and an intensity slider.
- **Effects belong to the cell + image pair** (Q&A #17): when the image of a cell changes, the
  cell's effects go back to their defaults (none active). Reset events (Q&A #19):

  | Event | Effects |
  |---|---|
  | The cell's image is replaced by another one (drop, Ctrl+V, browse) | Reset — none active |
  | An image is deleted | Reset for the images that shift into another cell |
  | Two cells are swapped | Kept — they follow the image, like rotation and zoom |
  | The layout changes | Kept — the fractions adapt to the new cell shape |

- **Selection changes** (Q&A #21): the selected effect is a property of the toolbar, not of the
  cell. When another cell is selected and the effect is active on it, it stays selected — its
  options and handles show at once for the new cell; when the effect is inactive on it, its
  button is released and the options row is hidden. With **no cell selected**, the effects
  toolbar stays visible but **disabled**; the options row and the handles are hidden.
- **Reset**: the cell's *Reset* tool also removes its effects, and is shown whenever an effect is
  active, even with no other action to undo (Q&A #13).

---

## Blur Effect — Bars and Snapping

- **Model**: the rectangle is stored as four fractions `Left < Right`, `Top < Bottom`, in `[0, 1]`
  of the **cell** (Q&A #8): zooming, panning or rotating the image does not move it. Stored with the effect's type and intensity on
  the cell's immutable state (an `Effects` part next to `ImageLook`, or inside it — implementation
  choice). Defaults on activation: **gaussian**, intensity at **mid-course** of the slider
  (Q&A #12).
- **Bars**: two vertical (left, right) and two horizontal (top, bottom), drawn on the **selected
  cell** only while the blur is the selected effect in the effects toolbar. Style: **full-length
  guides** crossing the whole cell, grabbable along their whole length, with a small grip at the
  middle of the rectangle's side (Q&A #16).
- **Drag**: each bar moves on its own axis only; the cursor is `SizeWE` / `SizeNS` over a bar. A
  bar never crosses its opposite one (a minimum gap is kept). Only the bars move: a drag inside the
  rectangle keeps the cell's current gestures (Q&A #11).
- **Snap**: while dragging, a bar within **6 px** of its cell edge lands **exactly** on it (fraction
  `0` or `1`), so the blur reaches the edge with no leftover strip (Q&A #10). Logical pixels on
  screen, scaled with `LogicalToDeviceUnits`.
- **Default rectangle** on activation: **centered, half the cell's width and height** — fractions
  `0.25 … 0.75` on both axes (Q&A #9).
- **Priority over existing gestures**: a press on a bar starts a bar drag before any pan / swap
  hit-test; elsewhere, the cell keeps its current gestures.

---

## Rendering

- **Hook**: `Compositor.DrawCell`, after the frame is drawn and still under the cell clip — so the
  preview, the still export, the animated export and live video frames all get the blur from one
  place.
- **Technique** (GDI+, no per-pixel loop, cheap enough for video):
  - draw the blurred part of the cell into a small offscreen bitmap (cell size ÷ factor), then draw
    it back stretched, clipped to the blur rectangle;
  - *Gaussian*: downscale with high-quality interpolation, upscale bilinear (two-step downscale
    when the factor is large, to avoid blockiness);
  - *Pixelate*: upscale with nearest-neighbour — the factor is the block size.
- **Resolution independence**: the intensity is expressed relative to the cell size, not in
  pixels, so the preview and an export at another size look the same.
- **Order**: the blur applies to the image as shown (after rotation, flips, zoom and grayscale),
  bands included when the rectangle covers them.

---

## Project Rules and Glossary

Requested by the user (Q&A #22): the toolbar and effect decisions of this workfile become
**general agent rules of the app**, valid for every future effect, and a **glossary** file fixes
the app's vocabulary. They live at the app root, as `RULES.md` and `GLOSSARY.md` (Q&A #23), and
are written **right away, ahead of the implementation go**, at the user's explicit request (Q&A
#24) — a user-authorized exception to the pre-implementation gate, limited to these two files.
`RULES.md` and `GLOSSARY.md` are the reference; the drafts below record what was written.

### Draft — effects rules (every effect)

- **Scope**: an effect belongs to a **cell + image** pair. Its state lives on the image's immutable
  state (next to `ImageLook`) and is not persisted.
- **Effects toolbar**: an always-visible row below the top bar, one toggle button per effect,
  **pressed** when the effect is active on the selected cell; disabled when no cell is selected.
  Nothing is added to the cell's hover toolbar.
- **Click semantics**: inactive → activates with its defaults and selects it; active, not
  selected → selects it; active and selected → deactivates it.
- **Options toolbar**: a row below the effects row, holding the selected effect's options only;
  hidden when no effect is selected.
- **Selection changes**: the selected effect stays selected when another cell is selected and the
  effect is active on it; otherwise its button is released and the options row hidden. No cell
  selected → toolbar disabled, options and handles hidden.
- **On-cell handles** of an effect (e.g. the blur bars) are drawn on the **selected cell** only,
  while that effect is selected; they take priority over the cell's other gestures only on their
  own hit area.
- **Snap**: a handle positioned relative to a cell edge snaps exactly onto it within 6 logical px.
- **Geometry** is stored in fractions of the cell, so it survives resizing and layout changes.
- **Rendering**: every effect is applied in `Compositor.DrawCell`, so the preview, the exports and
  video playback all show it; its strength is resolution-independent.
- **Reset**: the cell's *Reset* tool removes its effects too.
- **Reset events**: replacing a cell's image (drop, Ctrl+V, browse) or deleting an image resets
  the effects of the cells whose image changes; a swap or a layout change keeps them.

### Draft — glossary

| Term | Meaning |
|---|---|
| Grid (*grille*) | The whole composition, made of **cells** laid out by a **layout** |
| Cell (*cellule*) | One slot of the grid |
| Image | The content of a cell, whatever its source — still image, video, animated GIF, preview of a text / PDF or other file |
| Layout (*disposition*) | The arrangement of cells chosen in the layout strip |
| Selected cell | The cell the effects toolbar acts on |
| Effect (*effet*) | A per cell + image transformation toggled from the effects toolbar (first one: blur) |
| Effects toolbar | The top row of effect toggles |
| Options toolbar | The row below it, holding the selected effect's options |

---

## Test Impact

No unit test is created or updated: the user chose to keep the solution test-free (Q&A #14), as
every previous workfile did. Snap to edge, bar clamping and cell ↔ fraction conversion stay
untested by decision, not because nothing testable changes. Verification is manual: activate the
blur on a still image, drag each bar, snap each one to its edge, switch type and intensity, export,
then the same on a playing video; replace and delete an image to check the resets.

| Behaviour to pin | Test file | Create / Update |
|---|---|---|
| — (declined, Q&A #14) | — | — |

---

## Documentation

README: a short description of the effects toolbar — activate / select / deactivate an effect —
and of the blur: the four bars, the snap to the edge, type and intensity (Q&A #15).

---

## Open Questions

- [x] ~~Where does the *Effects* button live — in the per-cell hover toolbar, or in the main
  window's top bar acting on the selected cell?~~ → No Effects button: an always-visible effects
  toolbar at the top, its toggles pressed for the effects active on the selected image (Q&A #5)
- [x] ~~Do the effects and options toolbars (and the bars) stay pinned to their cell when the
  mouse leaves it?~~ → Nothing in the cell: the top toolbar stays visible and reflects the selected
  image (Q&A #6)
- [x] ~~Is the click semantics table right, in particular "active, not selected → selects"?~~ →
  Yes (Q&A #7)
- [x] ~~Is the rectangle anchored to the cell, or to the image (following zoom, pan, rotation)?~~
  → The cell (Q&A #8)
- [x] ~~Default rectangle when the blur is activated?~~ → Centered, half the cell (Q&A #9)
- [x] ~~Snap distance?~~ → 6 px (Q&A #10)
- [x] ~~Can the whole rectangle be moved by dragging inside it?~~ → No, bars only (Q&A #11)
- [x] ~~Default type and intensity?~~ → Gaussian, medium (Q&A #12)
- [x] ~~Does the *Reset* tool also remove the effects?~~ → Yes (Q&A #13)
- [x] ~~Unit tests — stay test-free, or create a test project for the pure logic?~~ → Test-free
  (Q&A #14)
- [x] ~~README — describe the Effects button and the blur?~~ → Yes, short (Q&A #15)
- [x] ~~Bar style — full-length guides across the cell, or the rectangle's edges only?~~ →
  Full-length guides (Q&A #16)
- [x] ~~When another image is selected, does the selected effect carry over, and what does the
  toolbar show with no image selected?~~ → Understood as "the image of the cell changes": the
  cell's effects reset — effects belong to the cell + image pair (Q&A #17). The intended question
  is asked again as Q&A #20
- [x] ~~Where does the effects toolbar sit relative to the existing top bar?~~ → New row below
  it (Q&A #18)
- [x] ~~Which events count as "the image of the cell changes" and reset its effects — replacing
  the image, swapping two cells, changing the layout?~~ → Replacement (drop, Ctrl+V, browse) and
  deletion reset; swap and layout change keep (Q&A #19)
- [x] ~~When **another cell** is selected, does the selected effect carry over, and what does the
  toolbar show with no cell selected?~~ → Carries over when active on the new cell (options and
  handles shown at once); no cell selected → toolbar disabled (Q&A #21)
- [x] ~~Where do the general effects rules and the glossary live?~~ → `RULES.md` and
  `GLOSSARY.md` at the level of `CLAUDE.md` (Q&A #23)
- [x] ~~Are they written now, ahead of the implementation go, or as part of the implementation
  run?~~ → Now (Q&A #24)

---

## Design Iterations

Chronological log of design refinements, of the choices the implementation run
took on its own — flagged `🧭 Implementation choices` — and of every adjustment
requested afterwards — flagged `⚙️ Post-implementation`. One entry per request,
in the order the requests were made.

### Iteration 1 — 2026-09-25

Initial design from the request and the scoping batch (Q&A #1–#4: per cell, intensity + type,
preview + export + live video, not persisted, straightforward subject → single scout pass).

Scout pass (three read-only agents): toolbars are plain WinForms panels in `MainForm` plus
owner-drawn per-cell tools in `GridPreview`; per-cell state is the immutable `ImageLook`;
`Compositor.DrawCell` is the single render choke point shared by preview, exports and video
playback; no test project exists. The pending `20260924-move-image-icon.md` workfile (not
implemented) changes the pan gesture — the bar drag takes priority over it either way.

Proposed: an owner-drawn effects row and options row, a four-fraction rectangle on the cell state,
a GDI+ downscale/upscale blur in `DrawCell`, and twelve open questions (Q&A #5–#16).

### Iteration 2 — 2026-09-25

The user dropped the *Effects* button of the initial request and anything drawn in the cell for
the toolbars (Q&A #5, #6): the effects toolbar is always visible at the top of the window and
reflects the selected image, its toggles pressed for the active effects. Click semantics confirmed
(Q&A #7); the rectangle is anchored to the cell (Q&A #8). The bars stay in the cell, on the
selected image, while the blur is selected. Two questions emerge from the new placement
(Q&A #17, #18).

### Iteration 3 — 2026-09-25

Bar behaviour and defaults settled (Q&A #9–#12): centered half-cell rectangle on activation, 6 px
snap to the cell edge, bars only (no whole-rectangle move), gaussian at medium intensity.

### Iteration 4 — 2026-09-25

Effects row on a new line below the top bar (Q&A #18), full-length guides (Q&A #16), *Reset* also
removes the effects (Q&A #13). Q&A #17 was read as "the image of the cell changes": effects belong
to the cell + image pair and reset when the image changes — a new rule, whose triggering events are
asked as Q&A #19. The intended question (another cell selected) is asked again as Q&A #20.

### Iteration 5 — 2026-09-25

Reset events settled (Q&A #19): replacing a cell's image (drop, Ctrl+V, browse) or deleting an
image resets the effects of the cells whose image changes; a swap and a layout change keep them.
Test-free (Q&A #14), short README section (Q&A #15, new *Documentation* section). Q&A #20 was not
understood; it is asked again with a concrete scenario as Q&A #21.

### Iteration 6 — 2026-09-25

The user asked (Q&A #22) for the toolbar and effect decisions to become general agent rules of
the app, valid for every effect, and for a glossary file (grid → cells → images, an "image" being
any cell content). New section *Project Rules and Glossary* with both drafts; location and timing
asked as Q&A #23, #24.

### Iteration 7 — 2026-09-25

Selection changes settled (Q&A #21): the selected effect carries over to another cell where it is
active; no cell selected → toolbar disabled. Rules and glossary: `RULES.md` and `GLOSSARY.md` at
the app root, next to where `CLAUDE.md` belongs (Q&A #23), written right away at the user's
explicit request (Q&A #24). The app has no `CLAUDE.md` yet, so nothing loads them automatically —
creating one that imports both is proposed separately, not done.

---

## Implementation Log

Which delivery steps are done, and in which iteration. A step that does not apply
says so rather than staying blank.

| Step | Iteration | Date | Notes |
|---|---|---|---|
| Code | | | |
| Unit tests | | | |
| README | | | |

---

## Q&A Log

Questions asked by the agent during design, with user responses.

| # | Question | Answer | Date |
|---|---|---|---|
| 1 | What does the blur mask apply to? | Per cell | 2026-09-25 |
| 2 | Which options does the blur options toolbar offer? | Intensity + type (gaussian / pixelate) | 2026-09-25 |
| 3 | Where must the blur be effective? | On-screen preview, image export, videos during playback — not persisted | 2026-09-25 |
| 4 | Is the subject straightforward or tricky / long? | Straightforward | 2026-09-25 |
| 5 | Where does the Effects button live? | No button in the cell: an always-visible toolbar at the top; its buttons are pressed for the effects active on the selected image. Active → click shows its options; click on the selected effect → deactivates; inactive → click activates | 2026-09-25 |
| 6 | Do the toolbars and bars stay pinned to the cell when the mouse leaves it? | Nothing in the cell; the top toolbar stays visible and reflects the selected effects | 2026-09-25 |
| 7 | Is the click semantics table right? | Yes | 2026-09-25 |
| 8 | Is the rectangle anchored to the cell or to the image? | The cell | 2026-09-25 |
| 9 | Default rectangle on activation? | Centered, half the cell | 2026-09-25 |
| 10 | Snap distance? | 6 px | 2026-09-25 |
| 11 | Move the whole rectangle by dragging inside it? | No, bars only | 2026-09-25 |
| 12 | Default type and intensity? | Gaussian, medium | 2026-09-25 |
| 13 | Does Reset also remove the effects? | Yes | 2026-09-25 |
| 14 | Unit tests? | Stay test-free | 2026-09-25 |
| 15 | README update? | Yes, short | 2026-09-25 |
| 16 | Bar style? | Full-length guides across the cell | 2026-09-25 |
| 17 | Selected effect on selection change, toolbar with no selection? | Read as "the image changes": everything goes back to defaults for the cell; the effect is per cell + image | 2026-09-25 |
| 18 | Effects toolbar placement relative to the existing top bar? | New row below it | 2026-09-25 |
| 19 | Which events reset a cell's effects (replace, swap, layout change)? | Replacement by another image (Ctrl+V / browse) and deletion of an image; not swap, not layout change | 2026-09-25 |
| 20 | Selected effect when another cell is selected, toolbar with no cell selected? | Not understood | 2026-09-25 |
| 21 | (Q&A #20 rephrased with a scenario) Blur selected on cell A, the user clicks cell B: are B's bars and options shown at once? | Yes, B shows its bars and options at once; no cell selected → buttons disabled | 2026-09-25 |
| 22 | (User request) Turn the toolbar / effects decisions into general agent rules, and add a glossary file | Requested: rules valid for every effect; glossary — a grid is made of cells, cells contain "images" (even a video or a text file preview) | 2026-09-25 |
| 23 | Where do the rules and the glossary live? | `GLOSSARY.md` + `RULES.md` at the same level as `CLAUDE.md` | 2026-09-25 |
| 24 | Written now, or during the implementation run? | Now | 2026-09-25 |
| 25 | Create an app-level `CLAUDE.md` importing `RULES.md` and `GLOSSARY.md`, so agents load them? | | |
| 26 | Implementation go? | | |

---

*Last updated: 2026-09-25*
