# Drop Zone

> Working document — a dedicated drop zone to the right of the preview, to add images without
> replacing the ones already placed.
> This file is the source of truth for the planned work until implemented,
> then the log of every adjustment made to it afterwards.

---

## Overview

Today, files dropped **onto a cell** of the preview replace that cell (intended, kept as is).
Files dropped elsewhere in the window are added like a paste — but once the preview fills most
of the window, "elsewhere" is a thin margin that is hard to hit. The user has no obvious target
to simply **add** images.

Goal: a permanent **drop zone** to the right of the preview canvas, whose only job is to add
images after the existing ones. It also opens a file picker when clicked.

Relevant components:

| File | Role |
|---|---|
| `UI/GridPreview.cs` | Paints the canvas (`CanvasBounds`, centered, 16 px margin, 1200 × 628 ratio), hit-tests cells (`CellAt`), highlights the external drop target (`ShowDropTarget`), `Add(images, targetCell)` |
| `UI/MainForm.cs` | Wires drag & drop (`OnDragEnter`, `OnPreviewDragOver`, `OnDragDrop`), `AddFilesAsync(paths, targetCell)` loads off the UI thread and reports skipped / ignored files in the status line |

---

## Current Behaviour (as found in the code)

- `OnDragDrop`: dropped on `_preview` → `DropCell(e)` (cell index or -1); dropped on the form → -1.
  `AddFilesAsync(paths, target)` then calls `_preview.Add(images, target)`.
- `Add` with `targetCell = -1`: images fill the free slots; the **first excess** image replaces
  the selected cell (else the last one); further excess is disposed and reported as ignored.
- Empty state: the whole canvas is a dashed rectangle reading
  *"Drop 1 to 4 images here, or paste them with Ctrl+V"*.
- `ImageLoader.TryLoadFile` has no extension filter: it tries to decode any file and reports
  failures as "skipped".

---

## Drop Zone — Behaviour

Agreed with the user (scoping batch):

- **Drop = append** after the images already loaded, never replacing a cell by target. Same path
  as a paste: `AddFilesAsync(paths)` with no target cell.
- **Always visible**, to the right of the preview canvas, and **highlighted while files are
  dragged over it** (same highlight colour as a targeted cell).
- **Click = file picker** (multi-select); the chosen files are added exactly like a drop.

Pending (see Open Questions): behaviour when the grid is full, visibility in the empty state.

---

## Drop Zone — Look and Placement *(proposal)*

- A vertical strip, the **height of the canvas**, on its right, separated from it by the canvas
  margin. The canvas shrinks horizontally to make room (it keeps its ratio and stays centered in
  the remaining area).
- Dashed border (same style as the empty state), a large **"+"** and a short label
  *"Add images"*. Hover: lighter border, hand cursor.
- Width: fixed, around 96 logical pixels (DPI-scaled).

## Code Impact *(proposal)*

- `GridPreview` paints and hit-tests the zone itself (one control, one coordinate space): a
  `DropZoneBounds()` next to `CanvasBounds()`, which reserves its width; a public
  `IsOverDropZone(Point)` for `MainForm`; `ShowDropTarget` gains a way to highlight the zone;
  a `DropZoneClicked` event.
- `MainForm`: `OnPreviewDragOver` highlights the zone instead of a cell when the cursor is over
  it; `OnDragDrop` sends -1 as target when dropped on the zone; `DropZoneClicked` opens an
  `OpenFileDialog` (`Multiselect = true`, image filter + "All files") and calls `AddFilesAsync`.

---

## Test Impact

The repository has **no test project** (declined in v1, Q&A #12 of `20260923-application-v1.md`).
The change is UI wiring only: the append logic (`GridPreview.Add` with no target) already exists
and is unchanged. Pending the Open Question on tests.

| Behaviour to pin | Test file | Create / Update |
|---|---|---|
| *(none — UI-only change, no test project; pending the answer)* | — | — |

---

## Open Questions

- [ ] Grid already full (4 images): the zone is shown **disabled** (greyed, drop refused), or it
      stays active and applies the existing excess rule (the first extra image replaces the
      selected cell, else the last one)?
- [ ] Empty state (0 image): the whole canvas is already a drop target — show the zone anyway
      (consistent layout), or hide it until the first image is placed?
- [ ] Look: vertical strip the height of the canvas with "+" and "Add images" (proposal), or a
      smaller square button-like zone vertically centered?
- [ ] Implementation: zone painted inside `GridPreview` (proposal), or a separate control docked
      to the right in `MainForm`?
- [ ] File picker filter: common image extensions (png, jpg, jpeg, bmp, gif, tif, tiff, webp)
      plus "All files", or "All files" only (the loader tries any file anyway)?
- [ ] Unit tests: none (UI-only change, no test project), or create a test project now?
- [ ] Interaction with `20260923-layout-variants.md`: its thumbnail row placement is still open
      (one option is "a vertical strip on the side") — keep the right side reserved for the drop
      zone, and rule out that option there?

---

## Design Iterations

Chronological log of design refinements, of the choices the implementation run
took on its own — flagged `🧭 Implementation choices` — and of every adjustment
requested afterwards — flagged `⚙️ Post-implementation`. One entry per request,
in the order the requests were made.

### Iteration 1 — 2026-09-23

Initial design. Scoping batch settled the core behaviour: drop appends, zone always visible and
highlighted during a drag, click opens a file picker, subject expected to be straightforward
(single exploration pass, done directly). Proposal: a vertical strip right of the canvas,
painted and hit-tested by `GridPreview`, wired in `MainForm`. Seven open questions listed.

---

## Implementation Log

Which delivery steps are done, and in which iteration. A step that does not apply
says so rather than staying blank.

| Step | Iteration | Date | Notes |
|---|---|---|---|
| Code | | | |
| Unit tests | | | Pending Open Question |
| README | | | |

---

## Q&A Log

Questions asked by the agent during design, with user responses.

| # | Question | Answer | Date |
|---|---|---|---|
| 1 | What does a drop on the new zone do: append after the existing images, or replace them all? | Append | 2026-09-23 |
| 2 | When is the zone visible: always and highlighted during a drag, only during a drag, or always with no effect? | Always, highlighted during a drag | 2026-09-23 |
| 3 | Does a click on the zone do something: open a file picker, or nothing? | Opens a file picker | 2026-09-23 |
| 4 | Is the subject straightforward, or tricky / long? | Straightforward | 2026-09-23 |

---

*Last updated: 2026-09-23*
